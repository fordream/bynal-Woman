<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:padding="0dp"
    android:id="@+id/pj_detail_support_m_main"
    android:background="@drawable/v3_bg"
    android:orientation="vertical" >
    <org.acv.bynal.views.HeaderView
        android:id="@+id/pj_detail_support_header"
        android:layout_width="match_parent"
        android:layout_height="wrap_content" >
    </org.acv.bynal.views.HeaderView>

	<ListView
        android:id="@+id/listview_support"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_below="@+id/pj_detail_support_header"
        android:divider="@null"
        android:listSelector="@drawable/transfer"
        android:dividerHeight="0dp" >
    </ListView>
</RelativeLayout>